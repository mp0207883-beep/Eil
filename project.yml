name: EliteIPA
options:
  bundleId: com.elite.ipa
  deploymentTarget:
    iOS: 15.0

targets:
  EliteIPA:
    type: application
    platform: iOS
    sources:
      - .
    info:
      path: Info.plist
    entitlements:
      path: EliteIPA.entitlements
